<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PlayListMakers_doing" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/setting_icons</item>
            <item name="colorOnPrimary">@color/background_main</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background_settings</item>
        <item name="colorSecondaryVariant">@color/setting_icons</item>
        <item name="colorOnSecondary">@color/text_color</item>
        <item name="android:textColorHint">@color/secondary_text</item>
        <item name="android:color">@color/backgroundPlaceholder</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:fillColor">#1A1B22</item>
    </style>

</resources>